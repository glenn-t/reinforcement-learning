# Generic helper functions for the agent class

choose_action = function(agent, board) {
  # Causes the agent to run it's own choose action function and return the result
  agent$choose_action(agent, board)
}